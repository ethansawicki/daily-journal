export const entryDate = () => {
    let dateEntry = `<label for="entryDate">Date</label>`
    dateEntry += `<input type="date" name="entryDate" class="entryForm--DateSelector" />`
    return dateEntry
}